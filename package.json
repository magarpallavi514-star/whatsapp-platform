{
  "name": "whatsapp-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps",
    "dev": "npm run dev -w backend & npm run dev -w frontend"
  }
}
